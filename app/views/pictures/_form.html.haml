= simple_form_for(@picture) do |f|
  = f.error_notification

  .form-inputs
    = f.input :title
    = f.input :image
    = f.input :position
    = f.association :gallery

  .form-actions
    = f.button :submit
